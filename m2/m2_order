Module 2.

1. m2_fetch_grid_pem.yml
   (will save cert in m2_sgrid_admin.pem)

2. m2_create_grid_authtoken_cert.yml
   (will use the certificate: validate_certs=true)

3. m2_create_grid_authtoken_module.yml  
   (uses storagegrid login module instead of raw api)
   

4. m2_create_grid_authtoken_raw.yml
   (uses raw api call instead of login module)

Related curl scripts:

1. curl/2_generate_pem.sh      

2. curl/2_get_token.sh 
